#include "main.h"

/**
 * puts2 - a function that prints every other character of a string
 * @str: pointer to s string of number
 * Return: void
 */
void puts2(char *str)
{
	int ;

	for (i = 0; str[i] != '\0'; i += 2)
	_putchar(str[i]);

	_puthar("\n");
	} 
	
