#include <stdio.h>
#include <stdlib.h>
/**
 * main - the main entry
 * @argc: the argument count
 * @argv: the argument vector
 *
 * Return: the program name
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
