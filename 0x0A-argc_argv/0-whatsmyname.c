#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: pointer to an array consisting of string
 * Return: always (0) on success
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
