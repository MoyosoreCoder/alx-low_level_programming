#include "main.h"
#include <stdio.h>
/**
 * main - entry point of program
 * @argc: the argument count
 * @argv: the argument vector
 *
 * Return: the argument passed to main
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
